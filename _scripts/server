#Quick and Dirty Jekyll/Compass setup by @michigangraham
#!/bin/sh
#
# Usage:
#   server
#   open http://localhost:4000

set -e
cd $(dirname "$0")/..

_scripts/bootstrap && bundle exec foreman start -f .Procfile
exit=$?
exit $exit
